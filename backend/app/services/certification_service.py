from sqlalchemy.orm import Session

from app.models.user import User


def recalculate(db: Session, user_id) -> None:
    """
    Recalcula e persiste a certificação do usuário.
    Stub para Etapa 3 — implementação completa na Etapa 4.
    """
    # A lógica completa (checklist + registros → nível) será implementada
    # na Etapa 4 junto com os modelos ChecklistResponse e Certification.
    pass
